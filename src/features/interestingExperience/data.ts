// Interesting experience texts now live in messages/{en,ru}.json under "interestingExperience.items".
// Kept as an empty marker file so existing imports don't break unexpectedly during refactor.
export {}
